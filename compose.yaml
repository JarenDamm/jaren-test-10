# BEGIN MANAGED: postgres-service
postgres:
  image: postgres:16
  environment:
    POSTGRES_PASSWORD: insecure
  ports:
    - "5432:5432"
# END MANAGED

# BEGIN MANAGED: redis-service
redis:
  image: redis:7-alpine
  ports:
    - "6379:6379"
# END MANAGED
